name: Build Spring Boot

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Deploy
      uses: akhileshns/heroku-deploy@v3.12.14
      with:
          heroku_api_key: 8449b9ff-d45d-42f9-8756-aab91094f5f0
          heroku_app_name: demo-github-action
          heroku_email: sonthuol240100@gmail.com
