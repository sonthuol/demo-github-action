name: Build Spring Boot

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Deploy
      uses: akhileshns/heroku-deploy@v3.12.14
      with:
          heroku_api_key: c9e60fc6-47f2-4fbe-9444-31ac72aa31e8
          heroku_app_name: demo-github-action
          heroku_email: sonthuol240100@gmail.com
          jdk_version: '17'
